# TODO.md — 项目创意/提案暂存

> 已评审但暂缓排期的提案记录在此。恢复排期时从对应 Idea 的"前置条件"开始。

---

## Idea-1: 光照视觉增强 L4 —— 平面真反射 / 真实阴影贴图（A、B 均已交付）

- **状态**：A（平面真反射）已交付；B（阴影贴图）可行性探针**通过**（死结有绕法，见 AGENTS.md L4-B 备忘），批次①（打通）与批次②（snap 调校）均已交付；C（SSAO）按评审结论不做。
  （L1-L3：L1 `acd9425`、L2 `d6e497a`、L3 `f47382e`；L4-A/B：见 git log）
- **本条目已完结**。后续微调（影子强度/边缘/水面影子等）走正常迭代，不再挂 TODO。
- **推荐顺序**：先 A（平面反射）后 B（阴影贴图）；C（SSAO）不做或仅在关闭平滑光照时作为替代。

### A. 平面真反射（半分辨率 Reflector）

- **原理**：镜像相机（沿水面 y=64 翻转）二次渲染全场景到独立 RT，水面片元投影 UV 采样，乘菲涅尔混入。
- **难点**：
  1. 每帧第二次全场景渲染（169 区块 × 3 mesh），几何成本 ×2，真实 GPU 约 +30-50% 帧时间，须独立子开关；
  2. 水面自反射递归——反射 pass 需排除全部 water mesh（建议新增 layer 2 作反射排除层，layer 1 已被天体占用）；
  3. 幸运简化：所有水顶面都在 SEA_LEVEL=64，可假设唯一反射平面，oblique near-plane 裁剪只需一套参数；眼睛在水中时整体禁用并防闪帧；
  4. `applyVoxelLightWater` 注入重构：RT 反射**替换**现有菲涅尔天空色项（有 RT 用 RT、无 RT 回退），需波纹 UV 偏移采样 + 屏幕边缘 clamp/fade，uniform 翻倍；
  5. 反射画面不走 bloom/色调映射（链路位置决定），反射里的太阳无光晕——廉价反射通病，接受；
  6. 软渲染环境完全不可测性能，CI 守不住性能回归。
- **工作量**：1 批次。风险集中在集成面，不碰 shader 内部。

### B. 真实阴影贴图

- **原理**：方向光渲全场景深度到 shadow map，地面片元采样压暗。
- **难点**：
  1. **MeshBasicMaterial 不接收阴影（死结级）**：需 `material.lights=true` 硬改 + onBeforeCompile 手工补 shadow chunk 链（`shadowmap_pars_vertex/fragment`、`shadowmap_vertex/fragment`、worldPosition/vNormal varying、`directionalLightShadows` 结构体），片元手工调 `getShadow()` 乘进体素光公式——**与 three 版本强耦合，升级 0.160 可能静默失效，这是"风险最高"的本质**；
  2. shadow camera 覆盖 96-192 格 × 高度 0-256（depth 精度稀释），**必须 snap-to-texel**（位置按 texel 取整）防走路阴影游移闪烁；
  3. 太阳连续移动无法缓存——每 N 帧更新 shadow map 摊薄成本（角速度 0.03°/s，10 帧无视觉差），但与太阳视觉位置有滞后需实测；
  4. 与体素光语义冲突（设计难点）：树冠下天光 12-13 + 阴影再压 = 双重变暗。解法 = 阴影权重随 skyL 衰减（`shadowW = uShadowStrength * vVoxelLight.x`，varying 已有），影子含义从"方块遮挡"变为"太阳直射差"——核心卖点也是最难调得不像 bug 的部分（正午影子缩成脚下一团）；
  5. bias/薄片：1m 立方 + cross 双面薄片 + 256 高度 range → acne 与 peter-panning 都会出，normalBias 实测调；夜晚月光阴影直接关；
  6. 额外一次全场景 depth pass（MeshDepthMaterial override 可隔帧）。怪物 MeshLambertMaterial 天生支持——验收时防"只有怪有影子"的完成度错觉。
- **工作量**：2 批次（①chunk 注入打通+静态验证；②snap-to-texel+隔帧+融合调校）。
- **失败模式**：①走不通（Basic 上 chunk 注入有隐藏依赖）→ 停在 L3。

### C. SSAO/GTAO（不推荐主做）

- 需深度+法线双 prepass（Basic 无 MRT 法线，第三遍场景或深度重建）；收益与烘焙 AO 重叠（体素 AO 已表达方块角落暗化，SSAO 增量仅大树冠/屋檐尺度）；成本半分辨率+去噪不低。若做：GTAOPass(three 0.160 自带)半分辨率 + 强度 ≤0.4 + 与平滑光照互斥。

### 共性工程问题（三项都躲不开）

1. "关闭档逐字节现状"回归基线重验（L2 曾因光材质提亮泄漏被迫引入 `GfxState` 门控）；
2. 帧预算叠加：完整档已 3 pass，L4 再 +1 次场景级渲染——可能需要帧时间监视自动降级子系统（本身也是工作量）；
3. B 方案把项目钉死在 three 0.160 的 shader chunk 结构；
4. 软渲染验证盲区：性能与闪烁类问题（阴影游移/反射边缘）只能靠用户真机复核，迭代周期变长。

### 前置条件（恢复排期时）

1. B 方案先做 **1 天可行性探针**：Basic + lights=true + 手工 shadow chunk 在最小场景跑通 `getShadowMask()`；探针不通过则 B 永久搁置；
2. 用户真机确认 L1-L3 完整档帧率可接受（当前软渲染数据无参考价值）；
3. 恢复时从 A 开始排期，B 视探针结果决定。

---

## Idea-2: P3 后选做机制池 —— 弓蓄力 / 箭联机 / 潜影盒 / 凋灵链 / 铁傀儡

- **状态**：主线完成（2026-09-12）——A 弓蓄力、B 箭矢联机、E 铁傀儡、C 潜影盒、D-① 凋零骷髅头颅均已交付（提交号见 git log）；**D 剩余凋灵 Boss+信标维持独立立项**（见 D 节）。P0-P3 生存链（燧石→挖矿→盔甲→经验→床→桶弓→动物→鞘翅→耕种，`6188f70`…`c285409`）已全部交付，"从出生到击败末影龙"核心缺口清零。
- **共同前置**：P3 全链真机复核通过；A/B/C/E 各自独立可插拔，D 建议独立立项。

### A. 弓箭蓄力伤害（工作量小，1 批次内）

- **现状**：`_shootArrow` 右键即射——固定初速 28 m/s、固定伤害 6，无蓄力语义。
- **方案**：右键**按住**期间 charge 0→1（update 累计），松手发射；初速与伤害按 charge 插值（如 vel=28×(0.3+0.7·charge)、dmg=2+round(charge·6)）；满蓄加拉弓 FOV 收窄/手部后拉提示。
- **难点**：`controls.mouseRight` 现为**瞬时开关**（handleMouseInput 消费后置 false）——蓄力需要"按住"语义，与现有右键交互（工作台/床/骨粉/放置）的单击语义并存，需引入独立的按住计时通道且互不误触（对准可交互物时单击优先）。

### B. 箭矢投射物联机同步（工作量中，1 批次）

- **现状**：`arrows` 数组纯本地——A 射的箭 B 看不见，伤害也只在射端结算（与"怪物事件同步"立项前的处境相同）。
- **方案**：事件式而非状态同步——`arrow_shot {x,y,z,dx,dy,dz}` C2S 广播，各端本地确定性积分（初速/重力/寿命一致即可视觉对齐）；命中怪由射端权威上报（复用 `sendMobAttack` 链）；钉墙/寿命消失本地各自处理，不做方块账本写入。
- **难点**：各端怪实体位置本地模拟存在微小偏差——伤害射端权威、视觉允许差异（与怪物同步同款妥协）；掉落经验只在射端。

### C. 潜影盒（工作量中，1-2 批次；先评审存档/协议扩展）

- **现状**：`shulker_shell` 物品已注册（末地船战利品），但潜影贝生物未实装、无盒体。
- **方案**：`shulker_box` 方块 = 27 槽随身容器。容器 UI/协议地基已备（ChestScreen + `container_set` 整箱同步），但"内容跟随物品"是**新语义**：box 物品自带内容（物品 `data` 字段序列化 27 槽），SaveSystem 与联机 drop/背包协议都要带内容——**存档 V3 风险点**，先评审再动。
- **连带**：破坏盒体内容不散落（进物品）；放置后容器账本与物品内容的一致性（last-write-wins 边界）。

### D. 凋灵链（工作量大，独立立项 3+ 批次）

- **状态（2026-09-12 更新）**：依赖链前段已就绪——凋零骷髅（下界要塞 10%/灵魂沙峡谷 45%/其余下界 15% 生成 ✓）、头颅掉落 `wither_skeleton_skull` 10%（Idea-2D-① 已交付）、灵魂沙（下界生成器已有斑块 ✓）。**剩余：凋灵 Boss（三段血条+弹幕+T 型召唤检测）+ 下界之星 + 信标（金字塔检测+玩家 buff 系统）**——体量 3+ 批次，维持独立立项，从 Idea-2 主线剥离。
- **召唤（预留）**：T 型灵魂沙×4 + 头×3 摆放检测（右键头触发，复用末地环 `_trySummonIronGolem`/`detectEndRing` 结构检测套路）。

### E. 铁傀儡（工作量中，1-2 批次）

- **地基**：村庄系统全实装（村民/房屋/生成生命周期/绳拴 AI ✓），缺的只是"护卫"角色。
- **方案**：passive 型护卫怪——① 村庄自动生成（村民数 ≥5 门控，挂 `updateVillageSpawns` 同款节拍）；② 玩家手工搭建（铁块 T 型+南瓜头，结构检测同末地环套路）。AI = 村民绳拴游荡 + 敌对怪/僵尸接近村民或玩家时反击（高伤害+大击退，可复用 attackMob 链）。
- **掉落**：铁锭 3-5（给"铁获取"添一条战斗路线）。
- **注意**：box-parts 模型（4 臂/2 腿/躯干/头）、MAX_PASSIVE/MAX_MOBS 名额策略需单独定（护卫不挤敌对名额）。

### 推荐顺序

A（小，手感补课）→ B（中，联机体验补课）→ E（中，村庄生态闭环）→ C（中，**先评审存档 V3/协议扩展**）→ D（大，独立立项；凋零骷髅部分可提前剥离）。

---

## Idea-3: 体验与性能补强池 —— 音频系统 / 主线程 Worker 化 / 服务器玩家档案

- **状态**：进行中（2026-09-12 起排期，顺序 A→B→C 已确认）。**A-① 音频底座+核心交互音效已交付**（`src/audio/AudioEngine.js` WebAudio 程序化合成：挖掘/放置/受击/食用/弓/箭命中，材质九类分路，惰性解锁，音量/开关进 Settings）；A-② 怪物音/脚步/BGM 待做；B（探针先行，失败止损转分帧）、C（独立 players 目录）未开始。三项互相独立可插拔。

### A. 音频系统（体验断层最大）

- **现状**：全代码库 0 处 `AudioContext`——无挖掘/放置/脚步/受击/怪物叫声/背景音乐，完全静音游戏。
- **方案**：WebAudio 程序化合成（延续"SVG 程序化纹理"零资产思路）：噪声/振荡器+包络生成音效——挖掘按方块材质变调、放置、受击、食用、箭矢、怪物低吼、环境风声/BGM 简单和弦循环。
- **难点**：AudioContext 须在首次用户交互后创建（浏览器自动播放策略）；音量/开关进 Settings（与视频设置同款持久化）；脚步等高频音效需节流。

### B. 主线程 Worker 化 + 网格压缩

- **现状**：地形生成、光照 BFS、网格构建全在主线程（无任何 Web Worker）；贪心合并仅水顶面，实体方块逐面生成。
- **方案**：地形生成+网格构建迁入 Worker（Transferable ArrayBuffer），主线程只做光照增量与 mesh 上屏；实体方块贪心合并作第二期。
- **难点**：LightEngine 增量更新与生成边界交互；ChunkMeshBuilder 依赖图集 UV 表需同步给 Worker；收益在渲染距离 12 时最大，需真机帧时间对比基线。

### C. 服务器侧玩家档案持久化

- **现状**：玩家背包/位置仅存客户端（联机风险表"存档互相覆盖"对策=仅开房者持久化），中途换机/重装即丢。
- **方案**：`server/store.js` 扩展玩家档案（背包/位置/血量/饥饿/经验，按 房间+昵称 键），进房下发、定期与退房落盘；与现有 welcome 重放/重连保物品逻辑对齐。
- **难点**：与客户端自模拟的冲突仲裁（last-write-wins 延续）；昵称伪造覆盖他人档案（信任场景接受，但需 op 清理入口）。

---

## Idea-4: 服务器运维池 —— 房间开关 / 世界备份 / 速率限制

- **状态**：暂缓（2026-09-12 评审新增，低优先顺手项）。单项工作量均小，可在任意批次间隙插入。

### A. 房间配置开关

- 现状：房间级只有白名单/op；PvP 伤害、怪物生成全局恒开。
- 方案：`config.roomSettings: {房间名: {pvp: bool, mobs: bool}}`，管理面板编辑；伤害链按开关短路（服务器中继过滤）。注意与 `isOperator` 权限及测试套件对齐。

### B. 世界备份导出

- 现状：`server/world/*.json` 只能手工拷贝。
- 方案：`GET /api/room/<name>/backup`（op 权限）导出 JSON 附件，可选周期自动备份保留 N 份。

### C. 消息速率限制

- 现状：局域网信任场景无限制。
- 方案：按连接令牌桶（chat/set_block/player_state 分级阈值），超限丢包+内存日志计数，管理面板可见。


