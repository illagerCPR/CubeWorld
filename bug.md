# CubeWorld 已知问题

## CW-1 — 水体在距玩家固定距离处出现直线分界(未解决)

**现象**:大片水域中,距玩家一定距离处出现笔直(呈区块对齐的阶梯直线)的水体分界;
分界一侧可透见水下地形,另一侧为均匀纯色。**分界距玩家的距离固定,每次加载都在
同一相对位置**——随玩家移动,分界跟随玩家移动。

**历史排查记录**(均已合入但未根治):

| 尝试 | 提交 | 结论 |
|---|---|---|
| 水材质 opacity 0.7→0.48 | `fbf83c5` | 缓解了"两片分离水体 α 叠加"(视线先后穿过近滩+远海)这一次要因素,分界变淡未消失 |
| 雾距随渲染距离收口 near=0.5d/far=0.95d | `b24d3ca` | 修复了 fog.far(160) > 加载圈(96) 的硬截断问题;但用户复核现象仍在 |

**待验证假设(按嫌疑排序)**:

- **H5(高嫌疑): 水材质的 fog 未生效**。`applyVoxelLight` 用 `onBeforeCompile` 注入
  共享 MeshBasicMaterial——若注入的 shader 片段未保留/未触发 three 的 fog chunk
  (`USE_FOG`),水面完全不吃雾:远处水保持饱和水色直到加载边界硬消失,而陆地正常
  渐隐入雾。佐证:用户首张截图(bugofwater.png)中 96 格外的水仍饱和蓝色,若雾生效
  应几乎全白。验证法:远处水面像素色 vs 同距离陆地像素色 vs `scene.fog.color` 对比;
  或 `waterMaterial.onBeforeCompile` 后检查 program 是否含 `fog_fragment`。
- **H1: 加载边界外是天空盒**(材质 `fog: false`,雾永远盖不住它)。若雾色 ≠ 天空盒
  低角度(地平线)颜色,边界处"全雾物体 vs 天空盒"产生直线色差。验证:采样天空盒
  地平线颜色与 `scene.fog.color` 是否一致;不一致则需把雾色改为地平线色。
- **H2: renderer clearColor 与雾色不一致**(天空盒未覆盖的仰角)。
- **H3: 水顶面贪心合并 quad 边缘**处"顶面+水侧面"叠加次数突变(与 α 相关,与距离无关,
  可能性低)。
- **H4: 用户端设置差异**(渲染距离/亮度)导致复现参数不同。

**复现要点**:海景、水平或小俯角视线、注意分界到玩家的距离是否恒定;
`?t=` 版本化模块下可在控制台取 `game.renderer.scene.fog` 与水面像素采样。

---

<!-- 新问题按 CW-2、CW-3 … 追加 -->
